import { render } from '@testing-library/react';
import { Ribbon } from './Ribbon';

describe('Ribbon component', () => {
  it('should render a Ribbon component', () => {
    render(<Ribbon />);
  });
});
